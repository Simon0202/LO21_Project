/*
#ifndef FACTORY_H
#define FACTORY_H

#include "litteraleabstraite.h"
#include "numerique.h"
#include "rationnel.h"
#include "reel.h"
#include "expression.h"
#include "complexe.h"

class Factory
{
public:
   Entier* make_entier(std::string str);
   Reel* make_reel(std::string str);
   Rationnel* make_rationnel(std::string str);
   Complexe* make_complexe(std::string str);
   Expression* make_expression(std::string str);
   LitteraleAbstraite* make(std::string str);
};

#endif // FACTORY_H
*/
